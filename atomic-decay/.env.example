# Atomic Decay Environment Configuration
# ==============================================================================
# 🚀 QUICK START:
#   1. Run: make new APP_NAME=your-app TARGET_DIR=../your-app
#      (This creates a new app in a separate directory with secrets auto-generated)
#   2. Update the LTI_* values below with your actual configuration
#   3. Run: make setup (to initialize the development environment)
# ==============================================================================
# 📝 NOTE: If using this template directly, copy this file: cp .env.example .env
#          Then run: make generate-secrets (to generate secure random secrets)
# ==============================================================================

# Database Configuration
# Note: Port 5433 is used for Docker PostgreSQL to avoid conflicts with local installations
# The database name should match your application name (e.g., my_app_dev)
# Docker containers must be running: docker-compose up -d postgres
DATABASE_URL=postgres://postgres:password@localhost:5433/atomic_decay_dev
TEST_DATABASE_URL=postgres://postgres:password@localhost:5433/atomic_decay_test

# Application Configuration
# RUST_LOG: Controls logging verbosity (trace, debug, info, warn, error)
# HOST: Interface to bind to (127.0.0.1 for localhost only, 0.0.0.0 for all interfaces)
# PORT: Port number for the web server
RUST_LOG=debug
HOST=127.0.0.1
PORT=8383

# LTI Configuration
# ==============================================================================
# ⚠️  IMPORTANT: Update these values for your LTI tool installation
# ==============================================================================
# LTI_ISSUER: The issuer URL of your LTI tool (must be HTTPS in production)
#   Example: https://my-lti-tool.example.com
LTI_ISSUER=https://CHANGEME-your-domain.example.com

# LTI_CLIENT_ID: Unique identifier for your LTI client (provided by LMS during registration)
#   Example: 125900000000000123
LTI_CLIENT_ID=CHANGEME-your-client-id

# LTI_DEPLOYMENT_ID: Deployment identifier (provided by LMS during registration)
#   Example: 1:dkljskldjskljdklsj
LTI_DEPLOYMENT_ID=CHANGEME-your-deployment-id

# Security Secrets
# ==============================================================================
# 🔐 CRITICAL: Generate secure random secrets before deploying to production!
#    Run: make generate-secrets
# ==============================================================================
# JWT_SECRET: Used for signing JWT tokens (min 32 characters recommended)
JWT_SECRET=CHANGEME-run-make-generate-secrets

# SESSION_SECRET: Used for session encryption (min 32 characters recommended)
SESSION_SECRET=CHANGEME-run-make-generate-secrets

# ==============================================================================
# Additional secrets are stored in config/secrets.json:
#   - jwk_passphrase: Passphrase for encrypting JWK private keys
# ==============================================================================